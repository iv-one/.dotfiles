# -- plugins

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin "arcticicestudio/nord-tmux"

# -- general -------------------------------------------------------------------

set -g default-command "${SHELL}"

# New Tmux Panes go to the same directory as the current panel
bind c new-window -c "#{pane_current_path}"
bind c new-window      -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind \\ split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Lower the default tmux delay, this makes tmux more responsive
set -sg escape-time 0

# disable repeat
set-option -g repeat-time 0

setw -g xterm-keys on
set -s focus-events on

set -q -g status-utf8 on
setw -q -g utf8 on

set -g history-limit 5000

# -- display -------------------------------------------------------------------

set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed

set -g set-titles on          # set terminal title

set -g display-panes-time 800 # slightly longer pane indicators display time
set -g display-time 1000      # slightly longer status messages display time

set -g status-interval 10     # redraw status line every 10 seconds

# activity
set -g monitor-activity on
set -g visual-activity off

# -- mouse ---------------------------------------------------------------------

set -g mouse on

bind-key -T copy-mode-vi WheelUpPane send -X scroll-up
bind-key -T copy-mode-vi WheelDownPane send -X scroll-down

# # Vi copypaste
setw -g mode-keys vi
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy" \; display-message "highlighted selection copied to system clipboard"

# -- bindings ------------------------------------------------------------------

# act like vim
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

set -g prefix2 C-s

# -- look and feel -------------------------------------------------------------

# set-option -ga terminal-overrides ",xterm-256color:Tc"
# set -g default-terminal "screen-256color"

# # start window numbers at 1 to match keyboard order with tmux window order
# set -g base-index 1
# set-window-option -g pane-base-index 1

# # renumber windows sequentially after closing any of them
# set -g renumber-windows on

# # Feel free to NOT use this variables at all (remove, rename)
# # this are named colors, just for convenience
# color_black="colour234"
# color_white="white" # 015

# # This is a theme CONTRACT, you are required to define variables below
# # Change values, but not remove/rename variables itself
# color_dark="$color_black"
# color_status_text="$color_black"
# color_main="colour108"

# # -- appearence and status bar -------------------------------------------------

# # set inactive/active window styles
# # set -g window-style 'fg=colour249,bg=black'
# # set -g window-active-style 'fg=colour253,bg=black'

# # set the pane border colors
# set -g pane-active-border-style fg=colour254
# set -g pane-border-style fg=colour108
# set -g status-bg colour254

# set -g mode-style "fg=default,bg=$color_main"

# # command line style
# set -g message-style "fg=$color_main,bg=$color_dark"

# # status line style
# set -g status-style "fg=$color_status_text,bg=$color_white"

# # window segments in status line
# set -g window-status-separator " "
# separator_powerline_left=""
# separator_powerline_right=""

# setw -g window-status-current-style "fg=$color_main,bg=$color_black"
# setw -g window-status-current-format "#[fg=$color_main,bg=$color_white]$separator_powerline_right#[fg=$color_dark,bg=$color_main] #I:#W #[fg=$color_main,bg=$color_white]$separator_powerline_left#[default]"
# setw -g window-status-format " #I:#W "

# # -- appearence and status bar -------------------------------------------------

# set-option -g status on
# set-option -g status-interval 2
# set-option -g status-keys vi
# set-option -g status-right-length 140
# set-option -g status-right-style default
# set-option -ag status-right "#[fg=green,bg=default] #{pane_current_path} #(kubectl config current-context) "
# set-option -ag status-right " #[fg=blue,bg=default]#(kubectl config view --minify --output 'jsonpath={..namespace}')"

run '~/.tmux/plugins/tpm/tpm'